spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/real_estate_db
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

jwt:
  secret: verySecretKeyForJwtTokenGenerationInCodingChallenge123
  expiration-ms: 86400000
